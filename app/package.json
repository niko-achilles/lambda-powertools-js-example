{
  "name": "lambda-powertools-example",
  "author": {
    "name": "niko-achilles",
    "email": "niko.achilles.kokkinos@gmail.com",
    "url": "https://github.com/niko-achilles"
  },
  "version": "1.0.0",
  "license": "MIT",
  "type": "commonjs",
  "dependencies": {},
  "scripts": {
    "watch": "tsc -p tsconfig.json --watch",
    "prebuild": "rm -rf dist && mkdir dist",
    "build": "tsc -p tsconfig.build.json",
    "dev": "node dist/manualTracedLambda.js"
  },
  "devDependencies": {
    "@aws-lambda-powertools/commons": "^1.4.0",
    "@aws-lambda-powertools/tracer": "^1.4.0",
    "@types/aws-lambda": "^8.10.108",
    "typescript": "^4.8.4"
  },
  "engines": {
    "node": "16.18.0",
    "yarn": ">=1.22.18"
  }
}
